# Browser-first static converter

We are building the first version as a static browser app that parses the PAT export locally and downloads a CSV. This keeps the user's file on the device, avoids installing software on locked-down corporate PCs, and gives us a simple deployment story: host the files on any web server and share a URL.
